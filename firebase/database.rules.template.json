{
  "rules": {
    ".read": false,
    ".write": false,
    "refresh": {
      "$key": {
        ".read": false,
        ".write": "$key === 'REPLACE_WITH_REFRESH_KEY'",
        ".validate": "newData.isNumber() && newData.val() === now"
      }
    }
  }
}
